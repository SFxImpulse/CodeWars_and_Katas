# Given a string, return 18 if that string is "Zach", otherwise return 0:

how_many_light_sabers_do_you_own = lambda name: 18 if name == "Zach" else 0

# Test on codewars failed because one of the tests did not include a function parameter, but the function still works as intended if you actually add all the arguments.