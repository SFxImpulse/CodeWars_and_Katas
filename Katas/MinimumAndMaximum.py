# Create two functions, one that finds the maximum value in an array, and one that finds the minimum value in an array

maximum = lambda arr: max(arr)

minimum = lambda arr: min(arr)

# Refactored

maximum = max

minimum = min