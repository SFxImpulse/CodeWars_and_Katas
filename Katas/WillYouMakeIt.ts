// Given 3 integers, determine whether you the factor of the second and third integers is greater than the first integer:

const zeroFuel = (distance: number, mpg: number, fuelLeft: number): boolean => fuelLeft * mpg >= distance;