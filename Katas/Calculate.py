# Write mathemtical operation functions:

import math

add = lambda x, y: x + y

subtract = lambda x, y: x - y

multiply = lambda x, y: x * y

divide = lambda x, y: x / y

square = lambda x, y: x ** y

square_root = lambda x: math.sqrt(x)